import React from 'react';
import PageDefault from '../components/pageLayouts/pageDefault'

const MainPageContainer = (props) => {
    
    return (
        <PageDefault page={props.page} />
    )
}

export default MainPageContainer